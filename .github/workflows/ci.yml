name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  GOWORK: "off"
  CGO_ENABLED: "0"

jobs:
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          check-latest: true
          cache: true
      # The tokens are signed by pyjwt, which nobody here wrote: a token this
      # repository both minted and verified would prove only that the two
      # halves agree with each other.
      - name: pyjwt
        if: matrix.os == 'ubuntu-latest'
        run: pip install --break-system-packages "pyjwt[crypto]" || pipx install "pyjwt[crypto]"
      - name: the judge is there
        if: matrix.os == 'ubuntu-latest'
        run: python3 -c "import jwt, cryptography; print('pyjwt is importable')"
      # And the judge must actually be reachable in this lane: a skip here
      # looks exactly like a pass.
      - name: the judge is there
        if: matrix.os == 'ubuntu-latest'
        run: python3 -c "import pyotp; print(pyotp.__name__, 'is importable')"
      - run: go vet ./...
      - name: gofmt
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          unformatted=$(gofmt -l $(git ls-files '*.go'))
          if [ -n "$unformatted" ]; then
            echo "::error::these files are not gofmt-clean:"; echo "$unformatted"; exit 1
          fi
      - run: go test -count=1 ./...
        env:
          # Ubuntu installs pyjwt above, so a skipped test there is a lane
          # passing for the wrong reason.
          OIDC_REQUIRE_JUDGE: ${{ matrix.os == 'ubuntu-latest' && '1' || '' }}

  race:
    name: race detector
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: "1"
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with: { go-version: stable, check-latest: true, cache: true }
      # ⛔ The judge belongs in every lane that RUNS the tests, not only the
      # one named "test": without it almost every test here skips, and a skip
      # is not a failure. OIDC_REQUIRE_JUDGE turns that skip into a failure
      # wherever the judge is supposed to be there.
      - name: pyjwt
        run: pip install --break-system-packages "pyjwt[crypto]" || pipx install "pyjwt[crypto]"
      - run: go test -race -count=1 ./...
        env:
          OIDC_REQUIRE_JUDGE: "1"

  coverage:
    name: coverage (the standard is 100%)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with: { go-version: stable, check-latest: true, cache: true }
      # ⛔ The judge belongs in every lane that RUNS the tests, not only the
      # one named "test": without it almost every test here skips, and a skip
      # is not a failure. OIDC_REQUIRE_JUDGE turns that skip into a failure
      # wherever the judge is supposed to be there.
      - name: pyjwt
        run: pip install --break-system-packages "pyjwt[crypto]" || pipx install "pyjwt[crypto]"
      - run: |
          export OIDC_REQUIRE_JUDGE=1
          FLOOR=90.0
          go test -coverprofile=cover.out -coverpkg=./... ./... >/dev/null
          go tool cover -func=cover.out
          cov=$(go tool cover -func=cover.out | tail -1 | awk '{print $3}' | tr -d '%')
          echo "coverage: ${cov}%  floor: ${FLOOR}%  standard: 100%"
          # Written as an `if` rather than `awk ... && { ... }`: in that form
          # the LIST's status is awk's, and when it is the last line of the
          # script the step inherits it -- so a lane that is comfortably above
          # its floor fails, saying nothing about why. It cost a run here.
          if awk -v c="$cov" -v f="$FLOOR" 'BEGIN{exit !(c+0 < f+0)}'; then
            echo "::error::coverage ${cov}% fell below the recorded floor ${FLOOR}%"
            exit 1
          fi
          awk -v c="$cov" 'BEGIN{if (c+0 < 100) printf "::warning::%.1f points short of the 100%% standard\n", 100-c}'

  arches:
    name: arch ${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goarch: [amd64, arm64, riscv64, loong64, ppc64le, s390x]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with: { go-version: stable, check-latest: true, cache: true }
      - run: GOARCH=${{ matrix.goarch }} go build ./...
      - name: Install qemu-user-static
        if: matrix.goarch != 'amd64' && matrix.goarch != 'arm64'
        run: sudo apt-get update -qq && sudo apt-get install -y -qq qemu-user-static
      - name: Test
        run: |
          case "${{ matrix.goarch }}" in
            amd64|arm64) go test -count=1 ./... ;;
            riscv64)  GOARCH=riscv64 go test -count=1 -exec=qemu-riscv64-static ./... ;;
            loong64)  GOARCH=loong64 go test -count=1 -exec=qemu-loongarch64-static ./... ;;
            ppc64le)  GOARCH=ppc64le go test -count=1 -exec=qemu-ppc64le-static ./... ;;
            s390x)    GOARCH=s390x   go test -count=1 -exec=qemu-s390x-static ./... ;;
          esac
